<config>
    <option name="database">
        <label>Base de données utilisée</label>
        <description>
            Cette clé indique le nom (en général un alias) de la
            base de données qui sera utilisée par le module
        </description>
        <type>database</type>
    </option>
    
    <option name="callback">
        <type>callback</type>
    </option>
    
    <action name="search">    
        <option name="sort" label="Ordre de tri" type="string" />
        <option name="max" label="Nombre max de notices à afficher" type="int" />
        <option name="equation" label="Equation par défaut si aucune n'est indiquée par l'utilisateur" multiple="true" />
        <option name="filter" label="Filtre appliqué aux équations" multiple="true" />
    </action>    
</config>