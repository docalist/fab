<loop on="$this->selection" as="$record">/*
    */<loop on="$record" as="$field,$value">/*
            */<if test="$value">$field{"\n"}{wordwrap(implode(Config::get("formats.$format.sep",'¤'),(array)$value))}{"\n"}/*
            */</if>/*
    */</loop>//{"\n"}/*
*/</loop>